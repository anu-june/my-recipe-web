# MCP (Model Context Protocol) Configuration
# This file defines external resources and servers that can be accessed via MCP.

mcpServers:
  confluence:
    command: node
    args:
      - d:/recipe-web/confluence-mcp-server/index.js
    env:
      CONFLUENCE_URL: https://anuthekkel.atlassian.net
      CONFLUENCE_EMAIL: anuthekkel@gmail.com
      CONFLUENCE_API_TOKEN: ${CONFLUENCE_API_TOKEN}  # Set in .env.local
      CONFLUENCE_SPACE_KEY: RECIPE
